# EcoSnap Sort

This is a Next.js application built in Firebase Studio that helps you sort waste correctly. Snap a photo of a trash item, and our AI will tell you which bin to put it in.

To get started, take a look at `src/app/page.tsx`.
